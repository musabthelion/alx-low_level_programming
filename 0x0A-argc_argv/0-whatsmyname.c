#include<stdio.h>
#include"main.h"
#include<stdlib.h>

/**
 * main - function call
 * @argv: vector
 * @i: integer
 * Return: 0
 */

int main(int i, char *argv[])
{
	i = 0;
	if (i == 0)
	{
		printf("%s\n", argv[i]);
	}
	putchar('\n');
	return (0);
}
