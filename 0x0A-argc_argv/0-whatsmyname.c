#include<stdio.h>
#include"main.h"
#include<stdlib.h>

/**
 * main - function call
 * @argv: vector
 * @argc: count
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i = 0;

	if (i == 0)
	{
		printf("%s %d\n", argv[i], argc);
	}
	putchar('\n');
	return (0);
}
